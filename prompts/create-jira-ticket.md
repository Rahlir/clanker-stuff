---
description: Create a new jira story ticket
argument-hint: "<assignment description>"
---
Your task is to create a new `jira` story ticket for the following task:

$@

Make sure to load the relevant "ticket house style" skill if it exists. Also
load the `jira` skill.

If you need more context about this task, ask - never guess. Then, draft the
ticket and present it to me via the `annotate_text` tool. Once approved, post
the ticket to `jira`.
