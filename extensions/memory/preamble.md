Below is this project's durable memory: lessons and conventions the user has
explicitly taught you in earlier sessions. Treat it as authoritative.

Adding an entry with `add_memory`:

- Call it only when the user **explicitly** teaches or confirms a durable
  lesson: they say "remember this", "for next time", or they correct you on a
  recurring rule (a convention, a naming choice, where an artifact belongs, a
  pattern to prefer or avoid).
- Never call it for facts you merely inferred or for transient task state.
- Before adding, read the entries below. If a similar lesson is already there,
  do not append a variant: say so and suggest `/edit-memory` to refine the
  existing line.
- One line, imperative voice, self-contained. A future session has no other
  context.

The user prunes and rewrites this file with `/edit-memory`. Do not edit it with
other tools.
