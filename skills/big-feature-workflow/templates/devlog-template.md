# <Feature Name> - Devlog

## Summary
<!--
Maintained by Review mode. Replaced (not appended) on each review pass.
Audience: agents or developers working on other services that consume or
interact with what changed here. Keep it tight and surface-focused.
-->

**Status:** <in progress | partial | complete | blocked | divergent>

**Outcome:**
<!-- 1-3 sentences. What does this feature/refactor now make possible or different? -->

**External changes:**
<!-- Things visible outside this service: API endpoints, message schemas,
     event payloads, DB columns exposed to others, CLI flags, config keys.
     Omit purely internal refactors. -->
- ...

**Unchanged surfaces worth noting:**
<!-- Explicit non-changes that consumer agents might otherwise assume changed.
     Remove if nothing relevant. -->
- ...

**Consumer impact:**
<!-- What other services / clients need to do, if anything. Remove if none. -->
- ...

---

## Session Log

<!-- Append a new entry per implementation session. Newest at the bottom.
     Never rewrite past entries. -->

### YYYY-MM-DD - <Short summary>

**Tasks completed:** 1.1, 1.2 (or `n/a - direct mode`)

**Changes:**
- <Short description of change> (`path/to/file`)
- ...

**Deviations from plan:**
<!-- Remove if none. -->
- ...

**Follow-ups:**
<!-- New tasks discovered, deferred work, or issues to revisit. Remove if none. -->
- ...

**Verification:**
- <commands run, e.g. `npm run type-check`, `pyright`> - <result>
